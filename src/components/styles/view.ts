export enum Whitespace {
  XXLARGE = 40,
  XLARGE = 30,
  LARGE = 20,
  MEDIUM = 16,
  REGULAR = 12,
  SMALL = 8,
  TINY = 4,
}

export enum Radius {
  VIEW = 3,
  CARD = 5,
}

export enum ViewHeights {
  HEADER = 70,
}

export enum ViewWidths {
  CONTAINER = 890,
  AUTH_CONTAINER = 550,
  USER_DROPDOWN_MENU = 200,
  MODAL = 300,
}

export enum IconSize {
  SMALL = 14,
  REGULAR = 18,
  LARGE = 22,
  XLARGE = 32,
}
