import * as React from 'react';
import Svg, { Rect } from 'react-native-svg';

const PlaceholderBankIconComponent = (): React.ReactElement => (
  <Svg width={26} height={26} viewBox="0 0 26 26">
    <Rect width={26} height={26} rx={3} fill="#eaeaea" />
  </Svg>
);

export default PlaceholderBankIconComponent;
